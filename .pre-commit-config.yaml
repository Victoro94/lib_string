repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v16.0.0  # version de clang-format
    hooks:
      - id: clang-format
        files: \.(c|h|cpp|hpp)$
        args: ['-style=file']
